# iMusafir Website

The site lives in **`package/package/`**. See the full run guide there:

**[package/package/README.md](package/package/README.md)**

Quick start:

```powershell
cd package\package
npm install
npm start
```

Open **http://localhost:3000**
